package com.wkq.database.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "HOME_TOP_BANNER_INFO".
*/
public class HomeTopBannerInfoDao extends AbstractDao<HomeTopBannerInfo, String> {

    public static final String TABLENAME = "HOME_TOP_BANNER_INFO";

    /**
     * Properties of entity HomeTopBannerInfo.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Key = new Property(0, String.class, "key", true, "KEY");
        public final static Property Data = new Property(1, String.class, "data", false, "DATA");
    }


    public HomeTopBannerInfoDao(DaoConfig config) {
        super(config);
    }
    
    public HomeTopBannerInfoDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"HOME_TOP_BANNER_INFO\" (" + //
                "\"KEY\" TEXT PRIMARY KEY NOT NULL ," + // 0: key
                "\"DATA\" TEXT NOT NULL );"); // 1: data
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"HOME_TOP_BANNER_INFO\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, HomeTopBannerInfo entity) {
        stmt.clearBindings();
 
        String key = entity.getKey();
        if (key != null) {
            stmt.bindString(1, key);
        }
        stmt.bindString(2, entity.getData());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, HomeTopBannerInfo entity) {
        stmt.clearBindings();
 
        String key = entity.getKey();
        if (key != null) {
            stmt.bindString(1, key);
        }
        stmt.bindString(2, entity.getData());
    }

    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0);
    }    

    @Override
    public HomeTopBannerInfo readEntity(Cursor cursor, int offset) {
        HomeTopBannerInfo entity = new HomeTopBannerInfo( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // key
            cursor.getString(offset + 1) // data
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, HomeTopBannerInfo entity, int offset) {
        entity.setKey(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setData(cursor.getString(offset + 1));
     }
    
    @Override
    protected final String updateKeyAfterInsert(HomeTopBannerInfo entity, long rowId) {
        return entity.getKey();
    }
    
    @Override
    public String getKey(HomeTopBannerInfo entity) {
        if(entity != null) {
            return entity.getKey();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(HomeTopBannerInfo entity) {
        return entity.getKey() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
